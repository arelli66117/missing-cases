import { useState } from 'react'
import { signInWithEmailAndPassword, createUserWithEmailAndPassword } from 'firebase/auth'
import { auth, firebaseEnabled } from '../lib/firebase'
import { LockKeyhole, UserRound } from 'lucide-react'
export default function Login(){const [register,setRegister]=useState(false),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[message,setMessage]=useState('');const submit=async e=>{e.preventDefault();if(!firebaseEnabled){setMessage('Demo mode: add Firebase credentials to enable secure sign-in.');return}try{await (register?createUserWithEmailAndPassword(auth,email,password):signInWithEmailAndPassword(auth,email,password));setMessage('Success — you are signed in.')}catch(err){setMessage(err.message.replace('Firebase: ',''))}};return <section className="grid min-h-[75vh] place-items-center px-5 py-12"><div className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft"><div className="grid h-14 w-14 place-items-center rounded-2xl bg-blue-50 text-brand"><UserRound/></div><h1 className="mt-6 text-3xl font-black text-ink">{register?'Create your account':'Welcome back'}</h1><p className="mt-2 text-slate-500">Sign in to manage reports and receive updates.</p><form className="mt-7 space-y-4" onSubmit={submit}><input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="w-full rounded-xl border px-4 py-3 outline-brand"/><div className="relative"><LockKeyhole size={16} className="absolute left-4 top-4 text-slate-400"/><input type="password" minLength="6" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-xl border py-3 pl-10 pr-4 outline-brand"/></div><button className="w-full rounded-xl bg-ink py-3 font-bold text-white">{register?'Create account':'Sign in'}</button></form>{message&&<p className="mt-4 rounded-xl bg-blue-50 p-3 text-sm text-brand">{message}</p>}<button className="mt-5 text-sm font-bold text-brand" onClick={()=>setRegister(!register)}>{register?'Already have an account? Sign in':'New here? Create an account'}</button></div></section>}
