import { Link, NavLink } from 'react-router-dom'
import { Menu, ShieldCheck, X } from 'lucide-react'
import { useState } from 'react'
const links=[['/','Home'],['/cases','Missing Cases'],['/report','Report'],['/dashboard','Admin']]
export default function Navbar(){const [open,setOpen]=useState(false); const style=({isActive})=>`text-sm font-semibold transition ${isActive?'text-signal':'text-slate-600 hover:text-brand'}`; return <header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 backdrop-blur"><nav className="mx-auto flex max-w-7xl items-center justify-between px-5 py-4"><Link to="/" className="flex items-center gap-2 font-bold text-ink"><span className="grid h-9 w-9 place-items-center rounded-xl bg-brand text-white"><ShieldCheck size={20}/></span>TRACE</Link><div className="hidden items-center gap-7 md:flex">{links.map(([to,label])=><NavLink key={to} to={to} className={style}>{label}</NavLink>)}<Link to="/login" className="rounded-xl bg-ink px-4 py-2 text-sm font-semibold text-white">Sign in</Link></div><button className="md:hidden" onClick={()=>setOpen(!open)} aria-label="Menu">{open?<X/>:<Menu/>}</button></nav>{open&&<div className="border-t bg-white px-5 py-4 md:hidden">{links.map(([to,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className="block py-3 font-semibold text-slate-700">{label}</NavLink>)}<Link to="/login" className="block py-3 font-semibold text-brand">Sign in</Link></div>}</header>}
